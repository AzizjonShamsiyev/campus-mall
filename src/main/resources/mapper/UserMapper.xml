<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.campusmall.mall.mapper.UserMapper">

    <select id="getAll" resultType="com.campusmall.mall.entity.User">
        SELECT * FROM users
    </select>

    <select id="getById" parameterType="long" resultType="com.campusmall.mall.entity.User">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.campusmall.mall.entity.User">
        INSERT INTO users(username, password, full_name, phone, email, role, status)
        VALUES(#{username}, #{password}, #{fullName}, #{phone}, #{email}, #{role}, #{status})
    </insert>

    <update id="update" parameterType="com.campusmall.mall.entity.User">
        UPDATE users
        SET username=#{username}, password=#{password}, full_name=#{fullName},
            phone=#{phone}, email=#{email}, role=#{role}, status=#{status}
        WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM users WHERE id=#{id}
    </delete>

</mapper>
