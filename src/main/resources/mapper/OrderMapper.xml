<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.campusmall.mall.mapper.OrderMapper">

    <resultMap id="OrderMap" type="com.campusmall.mall.entity.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="paidAt" column="paid_at"/>
    </resultMap>

    <select id="getById" resultMap="OrderMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="getAll" resultMap="OrderMap">
        SELECT * FROM orders
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders(order_no, user_id, total_amount, status, created_at, paid_at)
        VALUES(#{orderNo}, #{userId}, #{totalAmount}, #{status}, #{createdAt}, #{paidAt})
    </insert>

    <update id="update">
        UPDATE orders
        SET order_no=#{orderNo}, user_id=#{userId}, total_amount=#{totalAmount},
            status=#{status}, created_at=#{createdAt}, paid_at=#{paidAt}
        WHERE id=#{id}
    </update>

    <delete id="delete">
        DELETE FROM orders WHERE id=#{id}
    </delete>

</mapper>
