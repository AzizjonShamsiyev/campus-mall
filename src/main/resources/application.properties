# --- Server ---
server.port=8081

# --- Datasource ---
spring.datasource.url=jdbc:mysql://localhost:3306/campus_mall?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=www2005A
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- MyBatis ---
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.campusmall.mall.entity

# --- Logging ---
logging.level.root=INFO
logging.level.com.campusmall.mall=DEBUG
